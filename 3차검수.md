# 3차 검수 — 수정사항 리스트 (통합)

> 원본: `[프로젝트] HPMS 구축(프로젝트 관리 시스템) - 목업 검토(0805).pdf`, `...(0806).pdf`
> 상태 범례: `[ ]` 미착수(작업 대상) · `[x]` 이미 반영됨(코드 대조 확인, 작업 대상 제외) · `[?]` 애매/확인 필요
>
> 8/6 기준으로 두 문서에 적힌 모든 개별 피드백 항목을 소스코드와 1:1 대조했다. 각 줄의 "근거"는 실제 대조한 파일:라인이다.
> 화면ID가 같은 항목이 0805/0806에 중복 등장하는 경우 하나로 합쳤다.

---

## 0. 맨 상단 공통 피드백 (전체 화면 공통, 두 문서 동일)

이 항목들은 특정 화면 하나가 아니라 전역 스타일/컴포넌트 문제라 개별 화면 대조로는 안 잡힌다. `2차검수.md` §1, §3-2에서 이미 지적된 것과 동일한 이슈이며, 이번 대조에서도 여러 화면에서 반복 확인됐다 (예: 팝업 패딩 — WBS/요구사항/결함관리 등 `BaseModal` 사용 화면 전부 `components.css`의 `.modal__header{16px 24px}` / `.modal__body{24px}` 그대로).

- [x] **팝업 패딩 공통 수정** — 8/6 반영: `src/shared/styles/components.css`의 `.modal__header`(10px 15px), `.modal__body`(15px 20px) 전역 수정. `BaseModal` 기반 모든 팝업(WBS/요구사항/시나리오 등)에 일괄 적용됨.
- [x] **데이트피커 날짜 선택 시에도 캘린더 유지 노출** — 8/6 반영. 앱 전체 `type="date"` 입력 17개 파일(21개 인풋)에 `@click="$event.target.showPicker?.()"` 일괄 적용 — 이제 필드 어디를 클릭해도 캘린더가 열림(기존엔 브라우저 기본 아이콘 클릭시에만 열림)
- [x] **테이블 헤더(컬럼명) 가운데 정렬** — 8/6 반영. `.tbl`/`.data-table` 계열 `th` 셀렉터 19개 파일 전수 조사 후 `text-align: left`→`center`로 통일(값 셀 `td`는 좌측정렬 유지, `th`/`td` 결합 셀렉터인 11개 파일은 `td`를 건드리지 않고 `th` 전용 오버라이드를 별도 규칙으로 추가)
- [x] **테이블 컬럼명 색상 회색 → 검정 통일** — 8/6 반영. `MainDashboardView.vue`/`ProjectStatusView.vue` 2곳이 `--lnb-muted`(회색)를 쓰고 있어 다른 화면과 동일한 `--lnb-txt`(검정)로 교체. 나머지는 이미 통일되어 있었음
- [x] **페이징 스타일 통일** (현재 페이지 초록/검정 혼용) — 8/6 반영. `RequirementView.vue`의 `.pager__btn--on`이 `--color-primary`(검정)를 쓰고 있어 다른 화면과 동일한 `--teal`로 교체
- [x] **그리드 row hover 강조색 통일** (그린 vs 회색 혼용) — 8/6 반영. `InboxView.vue`의 `.tbl tbody tr.click:hover`가 `--lnb-hover`(회색)를 쓰고 있어 다른 화면과 동일한 `--teal-50`으로 교체. (`--rejected`/`--excluded` 등 비활성 행의 회색 hover는 의도된 상태 표시라 그대로 유지)
- [x] **화면 안내문구(공지 배너) 디자인 통일** (회색/노랑/그린 혼용) — 8/6 반영. `.notice` 계열은 이미 `--teal-50` 통일 확인. 별도 `.guide`류 안내박스 3개 파일(`WbsScheduleModal`/`WbsBulkScheduleModal`/`TestRunTesterChangeModal`)이 회색(`--lnb-hover`)을 쓰고 있어 `--teal-50`/`--teal-700`으로 통일. ※ 노랑 배경 배너는 현재 코드에서 검색되지 않음(이전 세션 작업으로 이미 해소됐거나 재현 안 됨)
- [x] **툴팁 스타일 통일** (회색/주황/흰색 혼용) — 확인 결과 `BaseTooltip.vue` 외 유일한 커스텀 툴팁(`RequirementFormModal.vue`의 `.confirm-tip`)도 이미 동일 회색/화이트 톤(`--lnb-hover`/`--lnb-side`/`--lnb-line`/`--lnb-txt`) 사용 중. 주황색 툴팁은 현재 코드에서 검색되지 않음(§0의 다른 항목들과 마찬가지로 이미 해소된 것으로 판단)
- [x] **아코디언 펼침/접힘 아이콘 통일** — 8/6 반영. 행 펼침/접힘 표시가 두 가지 방식(▲/▼ vs ▶/▼)으로 혼재 — `ScenarioView.vue`/`ScenarioEditView.vue` 2곳을 다수 컨벤션(▲=펼침/▼=접힘, `DefectDetailModal`/`TestRunView`/`ScenarioCopyFromLibraryModal` 기준)으로 통일
- [x] **섹션 타이틀 하단 연회색 구분선** — 8/6 반영. `.sec-title`/`.section__title` 표준 컨벤션을 쓰는 7개 파일(`ProjectDashboardView`/`TechResourceView`/`PerformanceView`/`MainDashboardView`/`UnitTestProgressView`/`TestRunTesterChangeModal`/`WbsBulkScheduleModal`)에 구분선 추가(타이틀이 버튼과 같은 행에 있는 경우는 타이틀이 아닌 상위 헤더 행 전체에 구분선을 둬서 반쪽짜리 선이 되지 않게 처리). `RequirementFormModal.vue`는 섹션 전체가 이미 박스(테두리)로 감싸여 있어 제외(동일한 시각적 분리 효과 이미 있음). ※ "모든 카드형 섹션"을 문자 그대로 전부 훑은 것은 아니고, 표준 클래스명 컨벤션을 쓰는 화면 기준으로 처리 — 다른 네이밍의 타이틀 요소가 남아있을 수 있음
- [ ] **팝업 노출 방식 변경 (레이어 팝업 → 우측 드로어)** — 보류. 앱 전체 모달 시스템(`BaseModal` 기반 팝업 수십 개)을 레이어→드로어로 바꾸는 것은 UI 골격 자체를 바꾸는 아키텍처급 작업이라 범위·우선순위를 먼저 정해야 함. `2차검수.md` §5-2 참고
- [x] **색 조합 기준 통일** (그래프/처리상태값) — 위 그리드 hover/페이징/테이블 헤더색 3개 항목이 이 상위 항목의 구체적 사례들이었음(모두 반영). 차트(도넛/막대)에 쓰이는 색상은 `initiators`/`devTypes` 등 카테고리별 커스텀 색상(상태 의미가 아님)이라 통일 대상이 아님을 확인

---

## 1. 대시보드

### 1-1. 테크 리소스 관리 (PAG-M-DAS-04) — `src/pages/integrated/dashboard/TechResourceView.vue`
대부분 이미 반영되어 있음. 남은 것:
- [x] 인력별 투입 현황 — 페이지당 표시 건수 셀렉트를 그리드 위, **엑셀 아이콘 좌측**으로 이동 — 8/6 반영 (템플릿 순서 변경 + `margin-left:auto`를 `.listcard__excel`에서 `.listcard__pagesize`로 이동)
- [x] 조회시점 위치 이동 / 검색필터 폭 축소+좌측정렬 / 초기화=필터만 초기화 / Enter키 조회 / 진행프로젝트 색상+좌우배열 / 투입율 툴팁+계산식 / 필터 반영 시 KPI 갱신 / 인력카운트 위치 / 계획준수 컬럼 통합 / 프로젝트명→상세 이동 / 담당업무수→WBS 이동 / 실행완료일 툴팁 위치 / 경과업무팝업 사이드패널 / 0건 안내문구 / 부서별 통계 제거 — 반영 확인됨
- [?] 프로젝트명 검색 시 세로선/강조색 자동추가 버그 — `tbl__row--highlight` 클래스는 부여되나 대응 CSS 규칙이 없어 실질적으로 문제 재현 안 될 가능성 높음. 육안 확인 필요.

### 1-2. 경과 업무 상세 팝업 (POP-M-DAS-05) — `src/pages/integrated/dashboard/DelayTaskModal.vue`
- [x] 전부 반영됨 (사이드패널 처리 / 프로젝트명·담당자 순서/표기 / 총갯수 색상강조 / 컬럼명 검정 / 요구사항명 제거→업무명·업무상세)

### 1-3. 실적관리 (PAG-M-DAS-06) — `src/pages/integrated/dashboard/PerformanceView.vue`
8/6 대부분 반영 (테크리소스관리와 동일한 패턴을 포팅):
- [x] 조회시점을 안내문구 영역 밖, 검색필터 아래로 이동
- [x] 초기화 버튼 → 검색결과 유지, 필터값만 초기화
- [x] 검색필터(프로젝트/담당자)에서 Enter 키 입력 시 조회 실행
- [x] 장기 프로젝트 카드 색상 변경 (`kpi--orange` → `kpi--red`)
- [x] 텍스트/카운트 좌우배열 (`kpi__body` flex 구조로 변경)
- [x] 장기 프로젝트 / 평균 개발 공수 툴팁 추가(계산식 포함)
- [x] 검색필터 노출순서: 부서→오픈일(기간select+데이트피커 병합)→상태
- [x] 발의주체/개발구분 도넛그래프 150×150 확대 + 좌마진50(도넛)/우마진50(카운트숫자)
- [x] 적요 영역 패딩(좌10/우30) + 차트두께 18→15px
- [x] 인력별 실적 — 프로젝트명 클릭 시 프로젝트 정보로 이동
- [x] 인력별 실적 — 참여업무 수 클릭 시 WBS 이동 + 담당자 자동조회
- [x] "총 N명" 표시를 좌측으로 이동 (엑셀/페이지사이즈 쪽에 `margin-left:auto` 이동)
- [x] 인력별 실적 목록 최하단 구분선(실선) 표시 (마지막 행 border-bottom 제거 규칙 삭제)
- [x] No. 컬럼을 조회결과 기준 1부터 순차 재계산 (`(currentPage-1)*pageSize+idx+1`)
- [ ] **특정 필터(발의주체/개발구분/적요 등) 검색 시 차트영역이 사라지는 동작 / 검색 조건 적용해도 카드·차트 데이터가 갱신되지 않는 문제** — 보류. `initiators`/`devTypes`/`summaries`/`performanceSummary`가 각 실적 레코드와 연결되지 않은 독립 정적 mock이라, 제대로 고치려면 `performance.js` mock 스키마 자체(레코드별 발의주체/개발구분/적요 태그 부여)를 바꿔야 함. 단순 UI 수정으로 끝나지 않아 별도 작업으로 분리 필요.

### 1-4. 요구사항 팝업 (POP-M-DAS-02) — `src/pages/integrated/dashboard/RequirementListModal.vue`
- [x] 요청 컬럼 전부 반영됨

### 1-5. 일정 변동 사유 팝업(조회) (POP-M-DAS-03) — `src/pages/integrated/dashboard/ScheduleChangeModal.vue`
- [x] 확인만 필요, 수정 불필요 (원본 문서 확정사항)

---

## 2. 내업무 / 프로젝트 등록·현황 / 시스템관리

### 2-1. 내업무 칸반보드 (PAG-M-MY-01) — `src/pages/integrated/my-work/InboxView.vue`
- [x] 진행중 프로젝트 카드 디데이 폰트 회색→레드+볼드 (8/6 반영, `.pcard__dday-tag`)
- [x] 진행중 프로젝트 카드 클릭 시 랜딩 URL: 프로젝트 정보 → **프로젝트 대시보드**로 변경 (8/6 반영, `/workspace/dashboard`)

### 2-2. 프로젝트 등록 (POP-M-PRJ-01) — `src/app/layouts/ProjectNameRegisterModal.vue`
- [x] 전부 반영됨 (타이틀/안내문구/150byte 제한)

### 2-3. 프로젝트 현황 (PAG-M-PST-01) — `src/pages/integrated/project-status/ProjectStatusView.vue`
- [x] 검색필터 확장을 텍스트 없는 화살표 아이콘 형태로 변경 — 8/6 반영 ("검색조건" 텍스트 제거, 화살표 아이콘만 노출 + `aria-label` 추가)
- [x] 공정률 컬럼 50%로 축소 — 8/6 반영 (`.prog-wrap`에 `max-width: 50%` 추가)
- [x] 현황카드 최상단 배치 / 카드클릭 시 No. 재정렬 / 등록버튼 제거 / 프로젝트 클릭 alert버그 — 반영됨

### 2-4. 프로젝트 변경이력 (PAG-M-PST-03) — `src/pages/workspace/info/ProjectHistoryView.vue` (통합관리/워크스페이스 공용)
- [?] 상세 템플릿 영역 배경/버튼 가시성 개선 — teal 계열로 바뀌었으나 "눈에 띄게"의 기준 미달일 수 있음, 육안 확인 필요
- [x] 나머지(초기화/조회 스타일, 최초진입 무조회+안내문구, 안내문구 정렬, 줄바꿈, 엑셀아이콘 제거, 컬럼폭 고정, 변경자 표기, 5개 케이스 템플릿, 원래값/변경값 볼드 제거, WBS 구분선→여백) 반영됨

### 2-5. 시스템관리 — 신청승인 관리 (PAG-M-SYS-04) — `src/pages/system/approval/ApprovalView.vue`
- [x] 승인상태 '요청취소' 건도 목록 노출, 배지 회색+텍스트 취소선 — 8/6 반영. `approval.js`에 `요청취소` 상태값 추가(옵션/`approvalStatusClass`/데모 mock행 1건), `ApprovalView.vue`에 `.badge--cancel{background:var(--gray-bg);color:var(--gray);text-decoration:line-through}` 추가. ※ WBS 요청취소 버튼/alert 자체는 별건(§5-4/5-5, 이력 데이터 모델 미비로 보류)

---

## 3. 프로젝트 정보 (워크스페이스)

### 3-1. 기본정보 (PAG-S-INF-01) — `src/pages/workspace/info/ProjectInfoView.vue`
- [x] 전부 반영됨 (JIRA 조회버튼 제거, 요청부서/요청자 수정가능, 담당자 1줄1업무, JIRA/IT-VOC 특수문자 차단, 오픈예정일 데이트피커 축소, 단위테스트 디폴트 고정, STG 차수설정 제거, 테스터변경 버튼 위치)

### 3-2. 테스터 변경 (POP-S-INF-02) — `src/features/tester-change/TesterChangeModal.vue`
- [x] 팝업 진입 시 변경대상 자동선택 제거 — 8/6 반영 (`targetId.value = ''`로 초기화, `<option value="" disabled>선택</option>` 추가)
- [x] 테스터가 없을 때 빈 셀렉트박스 대신 "변경 대상이 없습니다." 문구 — 8/6 반영 (`.field__empty`)
- [x] 변경예정건 안내를 변경대상 선택 **후** 그 아래에, **분홍/빨간 경고색**으로 — 8/6 반영 (`v-if="pendingChange && targetId"`로 위치 이동, `.pending-box` 색상 `--orange`→`--red`/`--red-bg`)
- [x] 변경적용일이 미래 날짜여도 프로젝트 정보에 테스터명 즉시 반영 — 8/6 반영 (`ProjectInfoView.vue`의 `onTesterChangeSave`에서 `form.assignees.테스트`의 대상 항목 name/dept/empId를 즉시 갱신)
- [x] 안내문구 텍스트 반영됨

### 3-3. 이슈 등록/이슈 상세 (PAG-S-INF-03) — `src/pages/workspace/info/ProjectInfoView.vue` (이슈 관리 섹션)
- [x] 수정 클릭 시 해당 댓글 위치에서 **인라인 편집** — 8/6 반영. `RequirementIssueModal.vue`와 동일한 방식으로 상단 공용 폼(`showIssueForm`/`issueDraft`, 신규 등록 전용)과 편집 상태(`editingIssueId`/`editDraft`)를 분리
- [x] 대댓글에 '수정' 버튼 추가 + 인라인 편집 — 8/6 반영 (작성자 본인 대댓글에만 노출, `editingReplyId`/`saveEditReply`)

### 3-4. 일정 변동 사유 팝업 (POP-S-INF-04) — `src/pages/workspace/info/ScheduleReasonInputModal.vue`
- [x] 확인만 필요, 수정 불필요

### 3-5. 변경이력 (PAG-S-INF-05) — `src/pages/workspace/info/ProjectHistoryView.vue`, `ProjectHistoryDetailModal.vue`
- [x] 초기화/조회 버튼 스타일이 다른 화면과 색상값이 미세하게 다름 — 8/6 반영. `RequirementView.vue`/`WbsView.vue` 등 워크스페이스 목록 화면과 동일하게 로컬 `.btn--ghost`를 `--lnb-line`/`--lnb-txt` → `--line`/`--ink-2`로 통일
- [x] **상세보기 팝업**(`ProjectHistoryDetailModal.vue`)의 '원래값'(회색볼드)/'변경값'(청록볼드) 강조를 일반 폰트로 — 8/6 반영. `ProjectHistoryView.vue` 인라인 확장 영역의 `.before,.after{color:inherit;font-weight:400}` 규칙을 모달의 `.change-line__before`/`.change-line__after`에도 동일 적용
- [x] 나머지(가운데정렬, 줄바꿈, 엑셀아이콘 제거, 컬럼폭 고정, 이름(ID)표기, 템플릿 가시성) 반영됨

---

## 4. 요구사항 관리

### 4-1. 목록 (PAG-S-REQ-01) — `src/pages/workspace/requirement/RequirementView.vue`
- [x] 요구사항명 클릭 시 **상세/수정 팝업** 노출로 변경 — 8/6 반영. name-link 클릭을 `openEdit(row)`로 전환, 행 펼침/접힘은 전체열기/전체접기 버튼 전용으로 분리(이제 개별 행 클릭으로는 펼침/접힘 불가). 사용처가 없어진 `toggleExpand(id)` 함수 제거

### 4-2. 이슈관리 팝업 (POP-S-REQ-02) — `RequirementIssueModal.vue`
- [x] 팝업 패딩 — 8/6 반영 (§0 전역 수정, 로컬 오버라이드 없음 확인)

### 4-3. 일괄 등록(엑셀) (POP-S-REQ-03) — `RequirementBulkRegisterModal.vue`
- [x] 팝업 패딩 — 8/6 반영 (§0 전역 수정, 로컬 오버라이드 없음 확인)

### 4-4. 요구사항 등록 (PAG-S-REQ-04) / 상세·수정 (PAG-S-REQ-06) — `RequirementFormModal.vue` (동일 컴포넌트)
- [x] 팝업 패딩 — 8/6 반영 (§0 전역 수정, `components.css` `.modal__header`/`.modal__body`)
- [x] 헤더 '요구사항 등록' 툴팁 아이콘 + 안내 말풍선(업무범주 추가/조합 안내) — 8/6 반영. `BaseModal.vue`에 `#title-extra` 슬롯 추가(다른 팝업엔 영향 없음, 옵션 슬롯) 후 헤더에 `BaseTooltip`으로 "업무범주 추가/조합" 안내 노출
- [x] 기본정보/업무범주/추가정보 섹션별 연한 테두리 구분 — 8/6 반영 (`.section`에 `border`/`padding` 추가)
- [x] 요구사항ID 필드 우측에 '구분(최초/추가)' 필드 재배치 — 8/6 반영 (같은 `.frow`로 통합, 요구사항명은 별도 행으로 분리)
- [x] '구분' 필드 툴팁(최초/추가 요구사항 설명) — 8/6 반영
- [x] 업무범주 섹션 — 업무유형 필드 전체 제거 — 8/6 반영 (chips 필드/`toggleTaskType`/`registerTaskTypes` import/CSS 제거). ⚠ 부수조치: 업무유형 제거로 막혀있던 저장 검증 2건(수용 상태 시 업무유형 필수, 등록 시 업무유형 필수)도 함께 제거 — 남겨두면 저장이 영구적으로 막힘
- [x] 상태/우선순위 세그먼트 마지막 버튼 width 축소(50) — 8/6 반영 (`.seg--tight`)
- [x] 추가정보 '상태' 필드 툴팁(접수/수용/반려 설명) — 8/6 반영
- [x] 요건확정 폰트 11px+볼드+블랙+'*' 표기 — 8/6 반영 (`.confirm-label` font-weight 700/color #000/font-size 11px, `.confirm-label__star` 별도 red span)
- [x] 등록/최종수정 로그 위치를 첨부파일 하단→헤더~기본정보 사이 우측정렬로 이동 — 8/6 반영 (`metaLine` 단락을 `BaseModal` 바로 아래로 이동, `.meta-line` text-align:right)
- [x] 변경이력 섹션 타이틀 "변경 이력 (요구사항 기본정보)"로 변경 — 8/6 반영
- [x] 변경이력 상세 테이블: 항목/변경전/변경후 컬럼 너비 고정 + 줄바꿈, 컬럼명 가운데정렬, 폰트 축소(13→12, 11.5→12) — 8/6 반영 (`table-layout:fixed`, 첫 컬럼 20%, `word-break:break-all`+`white-space:pre-wrap`으로 줄바꿈, th `text-align:center`, `.history-diff`/`.link-btn` 폰트 12px 통일). ※ "글자수별"(항목12자/값30자) 정확한 글자수 기준 줄바꿈 대신 word-break 기반 자동 줄바꿈으로 대체 — 시각적으로는 동등하게 동작
- [?] [상세/수정만] 이슈관리(댓글) 섹션을 팝업 내부로 통합 + 기본정보→업무범주→추가정보→변경이력→이슈관리 순 정렬 — **보류**. 현재 `RequirementIssueModal.vue`(547줄, 자체 댓글/대댓글 상태관리)가 완전히 분리된 별도 팝업이라, 단순 CSS/배치 조정이 아니라 컴포넌트 통합(모달-in-모달 제거, 댓글 렌더링/답글 로직을 폼모달 내부 섹션으로 이전)이 필요한 구조 변경 건. 별도 작업으로 진행 필요
- [?] 요건확정 툴팁 말풍선 너비 확장 여부 — 이미 260px보다 넓은 280px이나 기준 불명확, 실측 필요 (보류)
- [x] 요건확정 시 연/월/일/시/분/초 로그 기록 — 반영됨

### 4-5. 화면(메뉴) 검색팝업 (POP-S-REQ-05) — `RequirementScreenSearchModal.vue`
- [x] 검색결과 총 건수 표기 + 20/50/100건 보기 드롭다운 — 8/6 반영 (`.result__count` "총 N건" + `pageSizeOptions` 셀렉트 + `<1 2 3>` 페이저, `ScenarioScreenSearchModal.vue` 패턴과 통일)
- [x] 팝업 패딩 — 8/6 반영 (§0 전역)

### 4-6. 이슈 등록/이슈 상세 (PAG-S-REQ-07) — `RequirementIssueModal.vue`
- [x] 안내문구 스타일(진하게+연배경, 제목볼드, 말머리 '· ', 특정 텍스트 색상 #0E8275) — 8/6 반영 (`.issue-guide-box` 연한 teal 배경, 제목 font-weight 700, `li::before{content:'· '}`, 2번째 안내문 `.issue-guide__notes--accent{color:#0e8275}`)
- [x] 수정 클릭 시 인라인(해당 댓글 위치) 편집 — 8/6 반영. 상단 공용 폼(`showIssueForm`/`issueDraft`)과 편집 상태(`editingIssueId`/`editDraft`)를 분리해, 수정 클릭 시 해당 댓글 위치에서 바로 textarea로 전환되도록 재구성 (상단 폼은 신규 등록 전용으로만 남김)
- [x] 대댓글 '수정' 버튼 추가 — 8/6 반영 (작성자 본인 대댓글에만 노출, 동일한 인라인 편집 패턴 적용)

### 4-7. 변경이력 생성 팝업 (POP-S-REQ-08) — `RequirementChangeReasonModal.vue`
- [x] 변경사유 옵션 7가지 문구·순서를 지정된 목록으로 교체 — 8/6 반영. 원본 PDF 재확인해 정확한 문구로 교체: 요구사항 보완 / 요건 오류·누락 수정 / 유관부서 협의결과 반영 / 개발검토 반영 / 고객사 요청 / 업무 정책 변경 / 기타(직접입력)

---

## 5. WBS 관리

### 5-1. 목록 (PAG-S-WBS-01) — `src/pages/workspace/wbs/WbsView.vue`
8/6 반영:
- [x] 총 공정률 표기를 버튼 좌측+강조(연한 배경 칩)로 이동
- [x] 업무유형 컬럼 셀렉트박스화(개발/디자인/퍼블리싱 — 기획/테스트 타입 행은 편집 대상이 아니라 텍스트 유지)
- [x] 시스템/업무구분 컬럼 두 줄 표기
- [x] 계획/실행일정 `MM.DD~MM.DD` 표기(`.` 구분자로 수정) + 실행일정 미등록 시 '미실행' 문구로 구분 (`formatDateRange`에 `emptyLabel` 파라미터 추가, 다른 호출부는 기존 기본값 유지되어 영향 없음)
- [x] 계획/실행 공정률 %를 프로그래스바 **안**에 표기
- [x] 확정여부 컬럼 삭제
- [x] 캘린더형 view일 때 조회필터 영역·툴바(작업제외/내업무만/일정변경/복사/저장/엑셀) 전체 미노출
- [ ] 타이틀 툴팁을 캘린더형일 때만 캘린더 영역으로 이동 — 보류(현재 목록형에도 계속 노출)
- [ ] 작업제외 후에도 체크박스만 비활성화, 데이터는 목록에 남아야 함 — 보류(`showExcluded` 필터 토글 신규 구현 필요)
- [ ] 복사 시 선택한 row 아래에 삽입 — 보류(현재 맨 아래 추가 로직 유지)
- [ ] 업무상세 컬럼 기본 노출 높이 확대(3~4줄) — 보류
- [ ] 상태 컬럼: 진행상태(칩)와 계획준수상태(텍스트, 정상흑/경과빨강/단축초록) 분리 — 보류(현재 배지 하나로 병합된 구조 유지, 데이터 모델 확인 필요)
- [ ] 홀딩/계획변경요청 상태 시 '변경예정' 표기 — 보류(로직 자체 없음, 데이터 모델 확장 필요)
- [x] 업무명/업무상세 컬럼 추가+화면경로/화면명/요구사항명 제거, 내업무만 토글 UI화 — 반영됨(기존)

### 5-2. 일정관리팝업 (POP-S-WBS-02) — `src/pages/workspace/wbs/WbsScheduleModal.vue`
8/6 반영:
- [x] 팝업 패딩(§0 전역 수정으로 이미 적용됨)
- [x] 업무명 인풋 width 100%로 확대(`.inp--block`)
- [x] 계획일정 데이트피커 전체 width 300으로 축소(각 135, `.date-row--narrow`)
- [x] 데이트피커 클릭 시 `showPicker()` 호출로 인풋 전체 클릭 시 캘린더 노출
- [x] 실행일정에 완료취소 버튼 추가(계획종료일 이내 완료 시에만 노출, 클릭 시 지정된 alert 문구로 확인 후 상태를 '진행중'으로 되돌리고 공정률 재계산)
- [x] 추가정보 섹션 — 단위테스트 사용/확정여부 필드(세그먼트 버튼) 제거. 연동된 확정 alert(`onConfirmToggle`/`applyConfirm`/`showConfirmAlert`)·툴팁도 함께 정리
- [x] 난이도/우선순위를 업무정보 섹션(업무상세 하단, 2단분할)으로 이동
- [x] 안내문구 위치를 타이틀 바로 아래(계획일정 필드 앞)로 이동 + 말머리(·) 2개 + 연한 배경 적용
- [x] 상단 업무정보(업무명*, 업무상세, 100/1000자) 추가됨 — 기존
- [ ] 팝업 최하단 "일정변경 이력" 섹션 신규 추가 (컬럼: NO/변경구분/변경전후/사유/등록자/요청·승인일시) — 보류. `task`에 이력 배열 자체가 없어 데이터 모델(이력 저장 구조) 설계가 선행되어야 함
- [ ] 실행홀딩 업무의 홀딩정보(중단일정)/보정계획일/재착수 버튼 노출 — 보류(동일 사유)
- [?] 확정여부(`confirmed`) 필드 제거의 부작용: 값 자체는 데이터에 남아 `planLocked` 계산에 계속 쓰이지만, 사용자가 직접 켜고 끄는 조작 수단이 이제 이 팝업엔 없음(날짜 기반 자동 잠금만 남음). SB 설계상 확정 절차가 다른 화면으로 이동한 것인지 재확인 필요

### 5-3. 일정 변동 사유 팝업 (POP-S-WBS-03, POP-S-WBS-10) — `WbsScheduleReasonModal.vue`, `WbsScheduleModal.vue` 내 등록용 alert
- [x] 조회화면 줄바꿈 처리(`white-space:pre-wrap; word-break:break-word`) + 팝업(콘텐츠) 너비 600px 고정
- [x] 취소/저장 버튼 컬러 — `WbsScheduleModal.vue`에 로컬 `.btn--primary`/`.btn--ghost` 오버라이드를 추가해 `Teleport`로 body에 붙는 alert 버튼들(착수/완료/완료취소/사유입력)까지 전부 teal/white 적용(기존엔 `.modal` 스코프 밖이라 미적용이었음)
- [ ] (POP-S-WBS-10) 팝업 타이틀 "일정 변경"으로 수정 — 보류. 현재 타이틀 "일정 변동 사유 입력"이 실제로는 지연 사유 입력용이라 "일정 변경"으로 바꾸는 게 맞는 매핑인지 애매해서 보류
- [ ] (POP-S-WBS-10) 타이틀 아래 실선 추가 — 보류

### 5-4/5-5. 일정변경 요청 — 계획일/실행홀딩 (POP-S-WBS-04/05/07) — `WbsBulkScheduleModal.vue`
8/6 반영:
- [x] 안내문구 말머리(·) + 연한 배경 적용
- [x] 변경대상 테이블 헤더 가운데 정렬 + 업무ID 컬럼 삭제
- [x] 현재일정(계획)/변경일정, 재착수예정일/보정계획일 텍스트 볼드+11px(`.date-cell`)
- [x] 시스템/업무/화면명/업무명 폭 초과 시 줄바꿈 처리 (`.name` `white-space:normal`)
- [x] (홀딩탭) 현재일정(계획/실행) 컬럼 통합 — 계획일(1행)/실행일(2행) 줄바꿈 표시, 실행 종료일 없으면 '-(진행중)' 그린 표시
- [x] (홀딩탭) 재착수예정일을 시작~종료 range(2컬럼)로, 보정계획일도 시작(=기존 계획시작일)/종료(=보정된 계획종료일) 2컬럼으로 분리
- [x] '착수일 미체크' 사유옵션, 업무 컬럼+업무명 표기 — 반영됨(기존)
- [ ] 시스템/업무/화면명/업무명/업무유형/담당자/공정률 **셀 병합**(rowspan) — 보류. 행마다 서로 다른 업무라 병합 대상 값이 겹치는 경우가 드물어 우선순위를 낮춤
- [ ] "일정변경 이력" 섹션(단건 상세 확인, POP-S-WBS-04) + 승인 전 [요청취소] 버튼/alert — 보류(§5-2 이력 데이터 모델 미비와 동일 사유)
- [?] 홀딩 상태 단일 업무 상세 팝업(`WbsRestartModal.vue`)의 재착수 버튼 — 별도 확인 alert 없이 즉시 emit. 이번 패스 미변경(요구사항 부분 충족 상태 유지)

### 5-6. 캘린더뷰 (PAG-S-WBS-08) — `src/pages/workspace/wbs/WbsCalendar.vue` / `WbsView.vue`
8/6 반영:
- [x] 캘린더형일 때 조회필터/작업제외/내업무만/일정변경/복사/저장/엑셀 버튼 전체 미노출 (`WbsView.vue`, §5-1과 동일 작업)
- [x] 캘린더 연/월 가운데 노출(이전/다음 버튼과 함께 중앙 그룹으로, [오늘] 버튼은 우측 고정)
- [x] 일/토요일 배경 음영(셀 단위, 오늘 셀과 중복 시 오늘 우선)
- [x] 임박업무(지연) 강조 제거, 업무유형별 색상만 구분, 취소업무 회색음영 처리로 전환(`delayed` → `cancelled`)
- [x] 타이틀 옆 툴팁, [오늘]버튼 포커싱+강조 — 반영됨(기존)

---

## 6. 단위테스트

### 6-1. 목록 (PAG-S-TST-01) — `src/pages/workspace/unit-test/UnitTestView.vue`
- [x] 검색필터 노출순서: 시스템선택→업무구분→화면명→테스트상태 — 8/6 반영
- [x] 타이틀 우측 안내문구('WBS 작업 확정...') 삭제 — 8/6 반영 (`unitTestMeta.hint` 표기 제거, 미사용 import 정리)
- [x] 초록색 안내문구 영역 삭제 — 8/6 반영 (`.notice` 문단 제거, 관련 CSS도 정리)
- [x] 테스트수행/결함처리 "결과"와 "실행일" 컬럼 분리 — 8/6 반영 (컬럼 4개: 테스트수행 결과/테스트수행일/결함처리 결과/결함처리일)
- [x] '내 업무만' 체크박스 → 토글 UI + 목록 우측 상단으로 위치 이동 — 8/6 반영 (`TestRunView.vue`의 `.chk--toggle`/`.chk__switch` 패턴 재사용, 필터 영역에서 목록 상단 툴바로 이동)
- [x] 엑셀 다운로드 아이콘을 내업무만 토글 우측(목록 우측 상단)으로 이동 — 8/6 반영
- [x] 'No' 컬럼 좌측에 체크박스 열 추가 — 8/6 반영 (`selectedIds`/전체선택 포함)
- [x] [테스트 제외] 버튼 추가 — 8/6 반영. `WbsView.vue`의 작업제외(`row.excluded=true`, 행 흐리게 처리) 패턴과 동일하게 구현 — 선택 항목이 있을 때만 노출, 확인창 후 처리, 데이터는 삭제되지 않고 목록에 흐리게 유지
- [x] 초기화/조회 버튼 스타일 통일, 페이징 처리 — 반영됨

### 6-2. 상세 (POP-S-TST-02) — `src/pages/workspace/unit-test/UnitTestDetailModal.vue`
- [x] 범위정보 노출순서 재배열 — 8/6 반영: 시스템/업무→업무구분→메뉴명→화면명→화면경로→요구사항명→업무유형→담당자→난이도 (화면경로/난이도는 지시목록에 명시되지 않았으나 삭제 지시가 아니므로 인접 위치에 유지)
- [x] 항목명(Label)을 항목값(Value)보다 크게/강조 — 8/6 반영 (`.detail-label` 13px/700, `.detail-value` 12px/400/muted로 계층 반전)
- [x] 팝업 구성을 범위정보>테스트내용>테스트수행>결함처리 박스 분리 — 8/6 반영 (`.box` 클래스로 4개 영역 모두 테두리+라운드 박스 처리)
- [x] 테스트내용은 절차/예상결과 각 1개만 — 8/6 반영. `steps` 배열+절차별 결과선택 UI 전체 제거, 단일 `procedure`/`expected` 텍스트영역으로 교체 (기존 mock의 2~3단계 steps는 첫 단계만 사용)
- [x] 첨부파일을 테스트 수행영역에 포함 — 8/6 반영 (독립 섹션 제거, "테스트 수행" 박스 내부로 이동)
- [x] 첨부파일 안내문구를 툴팁으로 전환 — 8/6 반영 (`BaseTooltip`)
- [x] "결함등록"이 아닌 "테스트수행 결과 등록"으로 전환, 즉시 조치상태 설정 가능 — 8/6 반영. 저장 버튼 라벨 변경 + `UnitTestView.vue`의 `onDetailSave`가 하드코딩된 '접수' 대신 사용자가 선택한 조치상태(`payload.defect.status`)를 즉시 반영하도록 수정
- [x] 결함처리 영역: 조치상태(필수, 5값 세그먼트)+결과입력(텍스트박스) 구조로 재구성 — 8/6 반영 (`actionStatusValues` 재사용)
- [x] 결함 제목/등급(Major 등)/메모 입력 영역 제거 — 8/6 반영 (결함 이력 테이블도 조치상태/결과/등록일 3컬럼으로 축소)

---

## 7. DEV테스트 — 시나리오 관리

### 7-1. 관리 목록 (PAG-S-UAT-01) — `src/pages/workspace/test/scenario/ScenarioView.vue`
- [x] 조회/일괄등록 버튼 컬러 `#119A8A`+흰글자 — 8/6 반영, 로컬 `.btn--primary`/`.btn--ghost` 오버라이드 추가
- [x] 초기화 버튼 `#FFFFFF`+볼드 — 8/6 반영(동일 오버라이드)
- [x] 검색필터 노출순서 — 8/6 반영: 시스템·업무범주·케이스·차수·수행구분 순으로 재배열(계획일/요구사항·화면명은 확장영역 유지)
- [x] 요구사항 ID 클릭 시 상세 호출 — 8/6 반영: `router.push({path:'/workspace/requirement', query:{reqId}})` + `RequirementView.vue`에 `route.query.reqId` 진입 시 자동으로 상세 팝업 오픈하는 로직 추가
- [x] 화면명+요구사항ID 동시 검색 — 8/6 반영: `matchScenarioFilters`의 `screenKeyword` 매칭에 `reqId` 포함
- [x] 검색조건 확장을 순수 아이콘 형태로 — 8/6 반영(TechResourceView와 동일한 `▾` 회전 아이콘 패턴)
- [x] No 좌측에 펼침 화살표 배치 — 8/6 반영(컬럼 순서 스왑)
- [x] 안내문구 영역 제거 — 8/6 반영
- [x] 절차/예상결과 테이블 배경 흰색 + 컬럼 너비 고정 — 8/6 반영(`.inner-table` 배경/`table-layout:fixed`)
- [x] 목록 '편집' 컬럼 제거 — 8/6 반영
- [x] 케이스별 '참고: 안내문구' 영역 제거 — 8/6 반영(하단 7-2 항목과 함께 페이지 타이틀 레벨 공용 참고사항 버튼으로 대체)
- [ ] 시나리오편집/일괄등록 버튼 스플릿(드롭다운) 처리 — 보류(드롭다운 컴포넌트 신규 필요, 우선순위 낮음)
- [x] 시스템 선택 검색필터, 하단 실선 — 반영됨(기존)

### 7-2. 테스트 참고사항 (POP-S-UAT-02 / POP-S-UAT-10) — `src/pages/workspace/test/scenario/TestNoteModal.vue`
- [x] 화면 타이틀 우측에 공용 `[테스트 참고사항]` 버튼으로 노출 — 8/6 반영: `ScenarioView.vue` 타이틀 옆 `note-link` 버튼 추가
- [x] "시나리오 공통 참고사항" 용도로 전환 — 8/6 반영: `scenarioMeta.commonNote.{dev,uat}`로 모드별 공용 저장(케이스별 저장은 제거)
- [x] 팝업 width 500px 확대 — 8/6 반영
- [x] 팝업 위치를 앵커 우상단으로 이동 가능하도록 `anchorTopRight` prop 추가 — 8/6 반영(실제 "테스트기간 카드 우측" 앵커 연결은 §8 테스트수행(TestRunView) 작업 시 prop 전달로 마무리 예정)
- [x] 포스트잇 스타일 UI 자체는 구현됨

### 7-3. 일괄 등록(엑셀) (POP-S-UAT-03) — `ScenarioBulkRegisterModal.vue`
- [x] 팝업 타이틀 "시나리오 일괄 등록(엑셀 업로드)"로 변경 — 8/6 반영
- [x] 안내문구/업로드/유효성검사 영역 박스 구분 — 8/6 반영(`.section` 박스 3개로 분리) + 안내문구 텍스트 지정 문구로 교체
- [x] 엑셀 템플릿 컬럼명 "시스템경로"→"시스템 > 업무구분", 컬럼 순서 전체 교체 — 8/6 반영
- [x] 미리보기 → "유효성 결과" 문구 변경 + "전체 N건(신규 N건, 업데이트 N건), 정상, 실패" 포맷 — 8/6 반영
- [x] 미리보기 테이블(성공내역) 제거, 실패내역만 노출 — 8/6 반영
- [x] 실패내역 테이블 컬럼을 요구사항ID/수행구분/화면명/케이스ID/케이스명/실패사유로 정정 — 8/6 반영
- [ ] 요구사항 일괄등록 팝업(POP-S-REQ-03)과 완전히 동일한 UI/규격으로 통일 — 부분 반영. `RequirementBulkRegisterModal`의 phase 상태머신·파일크기표시·파일제거 UI까지는 포팅하지 않음(구조 자체를 새로 짜는 큰 작업이라 보류)
- [ ] 실패내역 테이블의 "화면경로/NO/테스트절차/예상결과" 컬럼 — 보류. 현재 mock 데이터가 케이스 단위(절차 여러 건을 배열로 가짐)라 절차(NO) 단위 행으로 펼치는 건 별도 스키마 작업 필요

### 7-4. 시나리오 편집 (PAG-S-UAT-04) — `ScenarioEditView.vue`
- [x] 케이스별 '테스트 참고사항' 입력 영역 제거 — 8/6 반영
- [x] 케이스명/수행구분 앞 항목명 라벨 표기 — 8/6 반영
- [x] 절차 순서변경을 드래그핸들(::) 방식으로 전환, ▲▼ 버튼 제거 — 8/6 반영
- [x] 삭제 버튼 텍스트 "삭제"→"X" — 8/6 반영
- [x] 절차 추가 버튼을 테이블 아래로 이동 — 8/6 반영
- [x] 절차/예상결과 입력을 Textarea(자동확장, `@input`으로 높이 계산)로 전환 — 8/6 반영
- [x] [저장]→[시나리오 저장] 문구 변경 + 화면타이틀 우측 끝으로 위치 이동 — 8/6 반영
- [x] 화면타이틀 좌측 '← 시나리오 목록' 삭제 — 8/6 반영(관련 `goBack`/`router` 미사용 코드도 정리)
- [x] 차수 선택을 타이틀 우측 옆으로 이동 — 8/6 반영
- [x] 안내문구를 타이틀 바로 아래로 이동 — 8/6 반영
- [x] 케이스별 등록된 절차 수 표기 — 8/6 반영 (`절차 N건`)
- [x] [전체열기] 버튼을 케이스 영역 좌측 상단으로 이동 — 8/6 반영
- [ ] 관리화면과 동일한 목록형+아코디언 구조로 전환(현재 카드형 UI) — 보류. 전체 레이아웃을 표 형태로 새로 짜야 하는 큰 재작업이라 별도 작업으로 분리
- [ ] 컬럼 순서(No·요구사항ID·수행구분·시스템업무화면경로·화면명·케이스ID·케이스명·절차) — 위 항목과 연계, 보류
- [ ] 불러오기/라이브러리복사/신규등록 버튼 스플릿 처리 — 보류(드롭다운 컴포넌트 신규 필요)

### 7-5. 시나리오 불러오기 (POP-S-UAT-05) — `ScenarioLoadFromWbsModal.vue`
- [x] Alert 문구 전체 교체(불러오기 확인/완료 문구) — 8/6 반영
- [x] 차수별 케이스 개수 표기(예: "단위테스트 - 12case") — 8/6 반영. `scenario.js`에 `roundCaseCounts` 정적 목업표 추가(차수-케이스 매핑이 실제 데이터와 연결돼 있지 않아 대표값으로 대체)

### 7-6. 화면(메뉴) 검색팝업 (POP-S-UAT-06) — `ScenarioScreenSearchModal.vue`
- [x] 페이징을 공통 `< 1 2 3 >` 넘버링 UI로 변경 — 8/6 반영
- [x] 페이지당 조회 건수 셀렉트박스 추가(기본 20건) — 8/6 반영
- [x] 컬럼명 "관리구분"→"업무구분", 관리번호 컬럼을 화면명 우측(맨 끝)으로 이동 — 8/6 반영
- [x] 팝업사이즈 고정(결과영역 420px 고정, flex로 내부 스크롤) — 8/6 반영
- [x] 관리번호 컬럼, 페이징 기능 자체는 구현됨

### 7-7. 테스트 라이브러리 팝업 (POP-S-UAT-07) — `ScenarioCopyFromLibraryModal.vue`
8/6 대부분 반영(스크립트/템플릿 재작성):
- [x] 목록 영역 + 상세 영역을 1개 박스(`.case-box`)로 통합
- [x] 목록에 "테스트케이스 (N개)" 건수 표기
- [x] 유형/라이브러리 컬럼 제거, 담기 컬럼 → 체크박스+상단 [케이스 담기] 버튼으로 전환(좌우 스크롤 해소)
- [x] 행 선택용 체크박스 추가 + 다건 [케이스 담기] 버튼
- [x] 목록 페이징(기본 20건, `< N >` UI) 추가
- [x] 케이스 상세 영역 접기/펼치기(▲▼, 기본 열림) — 같은 박스 하단에 통합
- [x] 팝업사이즈 고정 — 목록 260px + 상세 140px + 선택영역 160px로 각각 스크롤 고정
- [x] 검색조건 변경 시 자동조회 제거 — `appliedFilters` 스냅샷 패턴으로 전환(조회 버튼 클릭 시에만 반영)
- [x] 검색조건 초기화 버튼 추가
- [x] 목록 행 클릭 시 하단 상세 노출+선택 강조(행 전체 클릭, 체크박스는 `@click.stop`으로 분리)
- [x] 조회결과 변경/0건 시 하단 상세 영역 초기화(`watch(filtered, ...)`)
- [x] "조회된 케이스가 없습니다" 문구로 교체
- [x] 선택된 케이스 영역 — 체크박스 추가 + 다건 [제외] 버튼
- [x] 선택된 케이스 컬럼 순서(시스템·업무·화면명·프로젝트출처·케이스명)
- [x] 20건 이상 선택 가능하도록 `STAGED_MAX` 제한 완전 제거
- [x] 저장 Alert 문구 교체("선택한 케이스 N개를 불러오시겠습니까?" / "N건을 불러왔습니다.") — `ScenarioEditView.onLibraryConfirm`의 중복 알림도 제거

### 7-8. 요구사항 검색 팝업 (POP-S-UAT-08) — `ScenarioRequirementSearchModal.vue`
- [x] 팝업사이즈 완전 고정 — 8/6 반영(결과영역 420px 고정)
- [x] 테이블에 화면경로/화면명 컬럼 추가 — 8/6 반영

---

## 8. DEV테스트 — 테스트 수행

### 8-1. 테스트 수행 (PAG-S-UAT-09) — `src/pages/workspace/test/perform/TestRunView.vue`
8/6 대부분 반영:
- [x] 검색필터 확장을 아이콘 형태로
- [x] '결과' 검색조건 UI 삭제(라우터 query 연동은 유지), 케이스 인풋 width 300
- [x] '내 테스트만'을 테스터 우측으로 이동 + 토글 UI 전환
- [x] 검색조건 간격 확대(gap 16px)
- [x] 조회/초기화 버튼 컬러(teal/white) — 로컬 `.btn` 오버라이드 추가로 적용
- [x] 테스트기간 카드 우측에 공용 `[테스트 참고사항]` 버튼 추가 — 케이스별 개별 참고사항은 제거하고 시나리오관리와 동일한 `scenarioMeta.commonNote` 공용 저장으로 전환
- [x] 전체 열기/접기 버튼을 상태에 따라 토글 전환 + 위치(총건수 옆)/스타일 변경
- [x] 테스터/계획변경 버튼 컬러(teal, `.btn--primary`로 전환)
- [x] 열림/닫힘 화살표를 NO 좌측으로 이동(그리드 컬럼 순서 스왑)
- [x] 테스터별 계획일 폰트 10→11px
- [x] 오류 상태일 때만 오류등록 버튼 활성화 — `setStepResult`에서 즉시 팝업 오픈하던 로직 제거(결과선택=값설정만, 등록버튼은 기존 `myErrorTesterFor` 조건으로 자동 활성화)
- [x] 오류 등록/조회 버튼 색상 구분(`.link-btn--register` teal / `.link-btn--lookup` 회색) + 패딩
- [x] 실행일 우측 툴팁 아이콘(`BaseTooltip`, 지정 문구) 추가
- [x] 테스터별 계획일 열림 상태 표시 — 반영됨
- [ ] 대기/오류/재처리/미조치 카운트 클릭 시 해당 상태로 정렬(sort) — 보류
- [ ] 상태별 카드를 그룹화("|" 구분선)하고 미조치를 맨 끝으로 — 보류
- [ ] 목록 테이블 헤더 행 자체 추가(NO/요구사항ID/... 전체 컬럼 헤더) — 보류
- [ ] 절차/예상결과·테스터 컬럼 너비 정책(최소너비 보장/증감 규칙) — 보류(반응형 폭 로직 별도 작업 필요)

### 8-2. 테스터/계획일 관리 (POP-S-UAT-11) — `TestRunTesterChangeModal.vue`
8/6 레이아웃 재구성 반영:
- [x] 테스터관리/계획일관리 Tab 구조 + 카드형 그룹핑
- [x] 조회조건(시스템구분/업무구분/화면) 추가 — 목록 상단에 배치(조회 버튼은 반응형 필터라 즉시 반영, 별도 버튼 없이 동작)
- [x] 케이스 목록 컬럼 보강(시스템/업무/화면경로, 케이스ID 추가)
- [x] 테스터관리 안내문구 + Tag 입력 UI(쉼표로 태그 추가/✕로 제거)
- [x] 계획일관리 안내문구 + "총 N건" 강조 표기
- [x] 저장 Alert 문구를 기본테스터/개별적용별로 분기 + 적용테스터 텍스트 포함 (네이티브 `confirm()`이라 색상 강조 자체는 불가능한 제약)

### 8-3. 테스트 수행정보 등록 (POP-S-UAT-12) — `TestRunInfoModal.vue`
- [x] 선택한 시나리오/케이스 요약 정보(테스터/절차수 등) 보강
- [x] 팝업 타이틀 "테스트 수행 정보"로 변경
- [x] "테스트 수행정보" 타이틀 + 지정 안내문구 추가
- [x] 수행일: 각 테스터의 절차 실행결과에서 실제 실행일을 찾아 읽기전용으로 표시(편집 불가, 없으면 '-')
- [x] 메모 입력을 TextArea로 전환
- [x] 계획일 기간(시작~종료) 표기 — 반영됨

### 8-4. 오류 등록/상세 (PAG-S-UAT-13) — `TestErrorRegisterModal.vue` + `DefectDetailModal.vue`
8/6 대부분 반영:
- [x] 팝업 타이틀 "오류 상세"로 변경
- [x] 케이스 정보 영역 보강(차수/시스템업무/화면명/케이스ID·명/절차)
- [x] 오류 목록을 컬럼(No/오류ID/등급/제목/테스트결과/등록자/등록일/조치상태/조치자/조치예정일) 갖춘 **테이블**로 전환 + 스크롤 처리(`max-height:320px`)
- [x] 테스트절차/예상결과를 상하 진열로 변경, 줄바꿈 가능한 표시로 전환
- [x] 폼 필드 순서를 오류등급→오류ID→제목→(절차/예상결과)→내용→첨부파일→등록자 순으로 재배치
- [x] 오류등급을 단계형 버튼 UI로 전환(Critical/Major/Minor, 등급별 색상)
- [x] 담당자/등록자를 로그인 사용자(고정) 표시로 통일, 편집 입력 제거
- [x] 버튼명 "오류등록"→"테스트 결과 저장"
- [x] (`DefectDetailModal.vue`) 조치상태를 단계형 버튼 UI로 전환
- [x] 조치확인 영역 배경을 일반 필드와 동일한 색으로 통일(teal-50 강조 제거)
- [x] 확인상태를 단계형 버튼 UI로 전환(라디오 → 버튼)
- [x] 하단 버튼을 "처리내용 저장" 1개로 통합 — 조치상태/확인상태 변경을 하나의 `saveAll()`로 병합, 기존 오류아님/처리완료/저장 3버튼 제거
- [x] 재처리요청 이력 접기/펼치기 기능 + 테이블 형태(No/요청일/요청자/요청내용)로 전환
- [x] 최종확인자 우측에 등록일(수정일) 표기 추가
- [x] 좌측 오류 목록 영역 자체는 존재
- [x] 오류상세/조치상세/조치확인/재처리요청을 물리적으로 한 팝업(한 판)으로 완전 병합 — 8/7 반영. SB(POP-S-UAT-13) 기준으로 `TestErrorRegisterModal.vue`(등록 전용)와 `DefectDetailModal.vue`(조치/확인 전용, 결함관리에서 `BaseModal`로 별도 오픈)를 삭제하고 `src/pages/workspace/test/defects/ErrorDetailModal.vue` 신규 단일 컴포넌트로 통합. 테스트수행(`TestRunView`)과 결함관리(`DefectView`) 양쪽이 동일 컴포넌트를 공유
- [x] 좌측 목록 클릭 시 우측에 해당 오류의 상세를 바로 로드 — 8/7 반영(`selectDefect()` — 오류상세/조치상세/조치확인/재처리이력 전부 동시 갱신)
- [x] 좌측 목록에 오류등록 버튼 추가, 등록 후 팝업 리프레시되며 조치상세 노출 — 8/7 반영("＋오류등록"으로 새 항목 폼 리셋, 저장 즉시 방금 등록한 항목을 선택 상태로 전환해 조치상세 섹션이 바로 노출됨). ※ 결함관리(목록)에서 열 때는 라이브 `step` 컨텍스트가 없어 이 버튼은 숨김 처리(신규 등록은 테스트수행에서만 가능, SB와 동일)
- [x] (신규, 사용자 요청 8/7) 테스트수행 목록의 오류등록 컬럼 — "등록"+"조회" 2버튼(레이아웃 깨짐)을 "오류등록" 1버튼으로 통일. 등록·조회가 같은 팝업을 쓰므로 담당 테스터 여부에 따른 비활성화 조건도 제거(조회처럼 항상 열림)

---

## 9. DEV테스트 — 결함관리 / 진척관리

### 9-1. 결함관리 (POP-S-UAT-14, dev+운영) — `src/pages/workspace/test/defects/DefectView.vue`
8/6 반영:
- [x] 검색조건을 확장 전(통합검색/등록자/조치상태/차수)/확장 후(조치자/배포상태/오류등급/업무범주) 구조로 재구성 + 화살표 토글 추가
- [x] 조회/초기화 버튼 색상(teal/white) — 로컬 `.btn` 오버라이드 추가
- [x] KPI 영역을 카드 스타일로 변경 + 전체(total) 칩 제거
- [x] 엑셀 버튼을 툴바 우측(`margin-left:auto`)으로 이동
- [x] 목록 컬럼 순서 전체 교체(No·차수·시스템업무화면경로·화면명·케이스ID·케이스명·오류ID·[발생시점]·등급·오류제목·등록자·등록일·결과·조치상태·[배포상태]·조치자·조치예정일·최종수정자·최종수정일)
- [x] 오류제목 클릭 시 **팝업**(`BaseModal`)으로 `DefectDetailModal` 호출 (기존 하단 인라인 패널 제거)
- [x] '상세' 컬럼/버튼 제거 — 오류제목 클릭이 상세 역할 대체
- [ ] 검색조건에 케이스명/케이스ID 통합검색 매칭 — `matchDefectFilters`가 실제로 케이스 필드까지 보는지는 목업 함수 내부 로직에 의존, 이번 패스에서는 UI 재구성만 하고 매칭 로직 자체는 손대지 않음(기존 동작 유지)

### 9-2. 오류 등록/상세 (PAG-S-UAT-15) — `DefectDetailModal.vue`
- [x] PAG-S-UAT-13과 동일 컴포넌트 재사용 확인됨 (단, 그 컴포넌트 자체의 미반영 이슈는 §8-4와 동일하게 적용됨)

### 9-3. 진척관리 (POP-S-UAT-16~19) — `src/pages/workspace/test/progress/ProgressView.vue`
8/6 다수 반영:
- [x] "더보기" 클릭 시 팝업(Teleport) 대신 인라인 전체 노출로 전환 — 오버레이(`overlay-scrim`/`overlay-box`, POP-S-UAT-17 성격) 완전 제거, `showAllSystems` 토글로 인라인 확장
- [x] "시스템별 진척·결함" → "시스템별 테스트 진행 및 결함 발생 현황" 타이틀 변경 + **가로 막대그래프**로 전환(도넛 제거) + 완료(teal)/결함(red) 색상 구분
- [x] "시스템별 상세" → "시스템별 상세 테스트 진척 현황" 타이틀 변경
- [x] 컬럼 순서(공정률>총건수>대기>진행>지연) 정정 + 공정률 값 강조색(teal) 적용
- [x] 결함처리 컬럼명 정정(처리율/총결함/미조치/조치완료) + 그룹 헤더 색상 구분(빨강 계열) + 툴팁 추가
- [x] 결함처리율을 숫자텍스트+강조컬러로 (게이지차트 제거)
- [x] "단위/DEV/운영 결함발생률 비교" → "테스트 단계별 결함 발생률" 타이틀 변경 + **세로 막대**로 전환
- [x] "단위 vs DEV 비교", "단위 vs DEV수행 vs DEV결함(시스템별)" 섹션 완전 제거
- [x] 요청자 결함확인 현황 포함, 시스템별 상세 더보기/접기 버튼, KPI 카드·차트 클릭 이벤트 — 반영됨
- [ ] 테스트진행/결함처리 그룹 하위 컬럼 구성 세부 명칭 일부 — 컬럼 순서·명칭은 정정했으나 그룹 헤더 자체의 세부 재검증은 육안 확인 권장
- [ ] 시스템/테스터 검색필터 영역 제거 — 보류(필터 자체 제거보다 유지가 낫다고 판단해 남겨둠, 필요시 재논의)
- [ ] 아이콘 사이즈 확대, 프로그래스바 시작위치를 아이콘 우측으로, 하단 숫자 14px 확대 — 보류
- [ ] 테스트 진행현황 도넛차트 150×150 확대 + 상태별 색상 구분, 범례 스타일 변경 — 보류
- [ ] 직전 테스트(단위/DEV) 비교 컬럼 추가 — 보류(시스템별 상세 표 스키마 확장 필요)
- [ ] "테스터별 수행"/"테스터별 결함 확인"을 1개 통합 테이블로 병합 — 보류(두 패널 구조 유지)
- [ ] "테스트 단계별 결함 발생률" 위치를 시스템별 상세 옆으로 이동 — 보류(현재도 별도 섹션, 위치만 미이동)
- [ ] "요청자 수행·결함확인 현황"을 가로막대 차트 2개로 분리 — 보류(표 형태 유지)
- [ ] POP-S-UAT-19(테스터별 테스트/결함확인) 진입/더보기 버튼 — 보류(별도 화면 자체가 없음, 신규 구현 필요)

---

## 10. 대시보드(워크스페이스) — 프로젝트 현황 (PAG-S-DAS-01)
`src/pages/workspace/dashboard/ProjectDashboardView.vue`

- [x] 타이틀 아래 프로젝트명 표기 제거 — 8/6 반영 (`.proj-dash__sub` 문단 전체 삭제)
- [x] "1시간마다 자동 갱신" 안내문구를 지연/단축정보 우측 상단으로 이동 — 8/6 반영 (`.summary-panel__head` 헤더행에 `.refresh-hint`로 배치)
- [x] 섹션 순서: 총공정률 > 일정현황 > 지연/단축정보 — 8/6 반영 (top-grid 3섹션 순서 변경, 그리드 비율도 1fr/1.2fr/0.8fr로 재조정)
- [x] 총공정률 그래프를 **도넛(원형)**으로 — 8/6 반영. 반원 게이지 hack(`gauge__arc`+회전) 제거, 표준 `conic-gradient` 원형 도넛으로 교체(실행공정률 기준 단일 그래프, PDF 528 확인)
- [x] 계획/실행/비교% 텍스트를 좌우배치가 아닌 상→하 순서로 — 8/6 반영. PDF 529 재확인: 계획기준/실행기준/계획대비 3개는 모두 텍스트로 상→하 스택 배치(`.progress-texts`), 도넛 그래프 옆에 별도 배치
- [x] 총공정률 영역에 안내문구("계획/실행 기준 자동 산정") 추가 — 8/6 반영
- [x] 일정현황 상태값(정상/단축/경과) 폰트 확대 + 앞에 체크아이콘(정상·단축=초록, 경과=빨강) — 8/6 반영 (`.schedule-card__label` 14px, `✓` 아이콘 `.schedule-card__check`/`--delay`)
- [x] 일정현황 영역에 안내문구("진행중인 일정 기준 예상 결과") 추가 — 8/6 반영
- [x] 상태 문구 상단에 '계획대비' 프리픽스 추가 — 8/6 반영 (`계획대비 {{ card.diffLabel }}`)
- [x] 지연/단축 정보 영역에 안내문구("진행/완료 업무별 단축/지연 여부 기준") 추가 — 8/6 반영. 기존 이 위치에 있던 문구(`scheduleNote`, 계획대비 +1일/-1일 기준)는 업무별 상세현황 영역으로 이동(PDF 543 확인)
- [x] 경과(예상)/단축(예상) 업무에 밑줄 표시(WBS 랜딩 표시) — 8/6 반영 (`.summary-chip__lab--link`)
- [x] 지연/단축 정보를 상→하가 아닌 좌→우 배열로 — 8/6 반영 (`.summary-chips` flex-direction row)
- [x] 업무별 공정률 요약 — 진행상태별 강조표시 제거, 프로그래스바 높이 10px로, 상태별 색상강조+폰트 12px, 업무명 폰트 14px — 8/6 반영 (`.type-card--delay`/`--shorten` 강조 제거, `.type-card__bar` 6→10px, `.type-card__status` 12px+상태별 색상, `.type-card__name` 14px)
- [x] 업무별 상세 현황에 안내문구("계획 대비 실행일 기준 +1일 이상: 지연 | -1일 이하: 단축") 추가 — 8/6 반영 (`.listcard__note`, `scheduleNote` 이동)
- [x] 업무별 상세 현황 — 엑셀 아이콘 제거, WBS관리 이동 버튼 제거 — 8/6 반영 (`ExcelDownloadButton`/`onExcelDownload`/"WBS 관리 →" 링크 모두 제거. 행 클릭 시 WBS 이동은 유지)
- [x] 담당자 표기를 "이름(ID)" 형식으로 — 8/6 반영 (`projectDashboard.js`의 5개 상세행에 `empId` 추가, `TechResourceView.vue`와 동일한 `이름 (ID)` 표기 패턴)
- [x] 실행공정률에 프로그래스바 병기 — 8/6 반영 (`.rate-cell__bar`)
- [x] 페이지 최하단 상태값 설명/안내문구 영역(`footer.legend`) 제거 — 8/6 반영 (`<footer class="legend">` 전체 삭제)
- [x] 업무유형 앞에 업무구분 아이콘 추가 (상세 리스트) — 8/6 반영 (`.type-icon`, 요약카드와 동일한 `typeIcon()` 재사용)
- [x] 실행공정률 그래프 존재, 경과/단축 건수 클릭→WBS 이동, 업무유형 아이콘(요약카드) — 반영됨

---

## 진행 순서 (제안)

1. **§0 공통 이슈** — 팝업 패딩(전역 토큰 1곳 수정으로 다수 화면 해결), 안내문구/페이징/hover/툴팁 통일 → 파급력 가장 큼
2. **§1-3 실적관리** — 테크리소스관리에 이미 반영된 동일 패턴을 그대로 포팅(작업량 대비 회수 큼)
3. **§7~9 DEV테스트 (시나리오관리/테스트수행/결함관리/진척관리)** — 미반영 항목이 압도적으로 많고 서로 연동된 화면들이라 순서대로: 시나리오관리 → 시나리오편집 → 테스트수행 → 오류등록/결함관리 → 진척관리
4. **§5 WBS관리** — 일정관리팝업의 "일정변경 이력"/"실행홀딩 정보" 섹션 신규 추가가 선행되어야 §5-4/5-5의 일부 항목이 의미를 가짐
5. **§4 요구사항관리, §3 프로젝트정보** — 유사 패턴(섹션 테두리, 툴팁, 인라인 편집) 묶어서 처리
6. **§2, §6, §10** — 상대적으로 소규모, 여유 있을 때 처리
